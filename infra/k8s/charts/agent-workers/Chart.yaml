apiVersion: v2
name: agent-workers
description: A1 Agent Engine - Agent Workers (Temporal workers, no HTTP service)
type: application
version: 0.1.0
appVersion: "latest"
dependencies:
  - name: a1-lib
    version: "0.1.0"
    repository: "file://../../_lib"
