image:
  tag: "placeholder"

replicaCount: 1

config:
  LOG_LEVEL: "debug"
  PORT: "8092"
  MAX_MEMORY_MB: "512"
  MAX_CPU_CORES: "2"
  MAX_TIMEOUT_SECONDS: "3600"
  MAX_OUTPUT_BYTES: "67108864"

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

hpa:
  enabled: false

ingress:
  enabled: false
