image:
  tag: "placeholder"

replicaCount: 1

config:
  LOG_LEVEL: "debug"
  LLM_GATEWAY_URL: "http://llm-gateway:8083"

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "300m"
    memory: "256Mi"

hpa:
  enabled: false

ingress:
  enabled: false
