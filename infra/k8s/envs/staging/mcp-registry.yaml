image:
  tag: "placeholder"

replicaCount: 1

config:
  LOG_LEVEL: "debug"
  PORT: "8090"

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "150m"
    memory: "200Mi"

hpa:
  enabled: false

ingress:
  enabled: false
