services:
  # ------------------------------------
  # Orchestration: Temporal Cluster
  # ------------------------------------
  temporal:
    image: temporalio/auto-setup:1.24.2
    container_name: temporal
    entrypoint: ["/usr/local/bin/temporal", "server", "start-dev", "--db-filename", "/var/lib/temporal/temporal.db", "--ip", "0.0.0.0"]
    ports:
      - "7233:7233"
      - "8233:8233"
    volumes:
      - ./temporal_data:/var/lib/temporal
    networks:
      - agent-net

  # ------------------------------------
  # Datastores: Postgres & Redis
  # ------------------------------------
  postgres:
    image: pgvector/pgvector:pg16
    container_name: postgres
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=agentplatform
    volumes:
      - pg-data:/var/lib/postgresql/data
      - ../postgres/init.sh:/docker-entrypoint-initdb.d/init.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d agentplatform"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - agent-net

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - agent-net

  # Run all migrations once, then exit.
  migrate:
    image: pgvector/pgvector:pg16
    container_name: migrate
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../postgres/migrations:/migrations
      - ../postgres/migrate.sh:/migrate.sh
    environment:
      - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    entrypoint: ["bash", "/migrate.sh"]
    networks:
      - agent-net

  # ------------------------------------
  # Tier-1: Tool Registry  (port 8086)
  # ------------------------------------
  tool-registry:
    build:
      context: ../../
      dockerfile: services/tool-registry/Dockerfile
    container_name: tool-registry
    ports:
      - "8086:8086"
    environment:
      # Remove DATABASE_URL to fall back to in-memory store.
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # Tier-2: Skill Catalog  (port 8087)
  # ------------------------------------
  skill-catalog:
    build:
      context: ../../
      dockerfile: services/skill-catalog/Dockerfile
    container_name: skill-catalog
    ports:
      - "8087:8087"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # Tier-2: Skill Dispatcher  (port 8085)
  # ------------------------------------
  skill-dispatcher:
    build:
      context: ../../
      dockerfile: services/skill-dispatcher/Dockerfile
    container_name: skill-dispatcher
    ports:
      - "8085:8085"
    environment:
      - WORKFLOW_INITIATOR_URL=http://workflow-initiator:8081
      - BASH_EXECUTOR_URL=http://bash-executor:8092
      - SANDBOX_MANAGER_URL=http://sandbox-manager:8082
      - KG_SERVICE_URL=http://kg-service:8093
    depends_on:
      - bash-executor
      - sandbox-manager
      - kg-service
    networks:
      - agent-net

  # ------------------------------------
  # Tier-3: Sub-Agent Registry  (port 8084)
  # ------------------------------------
  sub-agent-registry:
    build:
      context: ../../
      dockerfile: services/sub-agent-registry/Dockerfile
    container_name: sub-agent-registry
    ports:
      - "8084:8084"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # Tier-4: Agent Registry  (port 8088)
  # ------------------------------------
  agent-registry:
    build:
      context: ../../
      dockerfile: services/agent-registry/Dockerfile
    container_name: agent-registry
    ports:
      - "8088:8088"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # API Gateway  (port 8080)
  # ------------------------------------
  api-gateway:
    build:
      context: ../../
      dockerfile: services/api-gateway/Dockerfile
    container_name: api-gateway
    ports:
      - "8080:8080"
    environment:
      - AGENT_REGISTRY_URL=http://agent-registry:8088
      - WORKFLOW_INITIATOR_URL=http://workflow-initiator:8081
      - WORKFLOW_SERVICE_URL=http://workflow-service:8094
    depends_on:
      - agent-registry
      - workflow-initiator
      - workflow-service
    networks:
      - agent-net

  # ------------------------------------
  # Workflow Initiator  (port 8081)
  # ------------------------------------
  workflow-initiator:
    build:
      context: ../../
      dockerfile: services/workflow-initiator/Dockerfile
    container_name: workflow-initiator
    ports:
      - "8081:8081"
    environment:
      - TEMPORAL_HOSTPORT=temporal:7233
      - AGENT_REGISTRY_URL=http://agent-registry:8088
    depends_on:
      - temporal
      - agent-registry
    networks:
      - agent-net

  # ------------------------------------
  # Sandbox Manager  (port 8082)
  # ------------------------------------
  sandbox-manager:
    build:
      context: ../../
      dockerfile: services/sandbox-manager/Dockerfile
    container_name: sandbox-manager
    ports:
      - "8082:8082"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - litellm
    networks:
      - agent-net

  # ------------------------------------
  # Bash Executor  (port 8092)
  # ------------------------------------
  bash-executor:
    build:
      context: ../../
      dockerfile: services/bash-executor/Dockerfile
    container_name: bash-executor
    ports:
      - "8092:8092"
    environment:
      - PORT=8092
      - MAX_MEMORY_MB=512
      - MAX_CPU_CORES=2
      - MAX_TIMEOUT_SECONDS=3600
      - MAX_OUTPUT_BYTES=67108864
    networks:
      - agent-net

  # ------------------------------------
  # Agent Workers (Temporal Python)
  # ------------------------------------
  agent-workers:
    build:
      context: ../../
      dockerfile: services/agent-workers/Dockerfile
    container_name: agent-workers
    restart: on-failure
    environment:
      - TEMPORAL_HOSTPORT=temporal:7233
      - LITELLM_BASE_URL=http://litellm:4000/v1
      - LITELLM_API_KEY=sk-litellm
      - ANTHROPIC_BASE_URL=https://llm-inference.internal.angelone.in
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-sk--d-AWAtSYtikG7GvpvGNkw}
      - TEMPORAL_TASK_QUEUE=default-tenant-agent-queue
      - SANDBOX_MANAGER_URL=http://sandbox-manager:8082/api/v1/execute
      - SKILL_DISPATCHER_URL=http://skill-dispatcher:8085
      - TOOL_REGISTRY_URL=http://tool-registry:8086
      - WORKFLOW_INITIATOR_URL=http://workflow-initiator:8081
      - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      - temporal
      - litellm
      - sandbox-manager
      - tool-registry
    networks:
      - agent-net

  # ------------------------------------
  # Agent Workers (System)  (platform-system-agent-queue)
  # ------------------------------------
  agent-workers-system:
    build:
      context: ../../
      dockerfile: services/agent-workers/Dockerfile
    container_name: agent-workers-system
    restart: on-failure
    environment:
      - TEMPORAL_HOSTPORT=temporal:7233
      - LITELLM_BASE_URL=http://litellm:4000/v1
      - LITELLM_API_KEY=sk-litellm
      - ANTHROPIC_BASE_URL=https://llm-inference.internal.angelone.in
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-sk--d-AWAtSYtikG7GvpvGNkw}
      - TEMPORAL_TASK_QUEUE=platform-system-agent-queue
      - SANDBOX_MANAGER_URL=http://sandbox-manager:8082/api/v1/execute
      - SKILL_DISPATCHER_URL=http://skill-dispatcher:8085
      - TOOL_REGISTRY_URL=http://tool-registry:8086
      - WORKFLOW_INITIATOR_URL=http://workflow-initiator:8081
      - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
      - BEARTYPE_DOOR_DISABLE_CHECKER=1
      - BEARTYPE_ERROR_ON_NONFATAL_VIOLATION=0
    depends_on:
      - temporal
      - litellm
      - sandbox-manager
      - tool-registry
    networks:
      - agent-net

  # ------------------------------------
  # System Agent Seeder (one-shot)
  # ------------------------------------
  seed-system-agents:
    image: curlimages/curl:latest
    container_name: seed-system-agents
    depends_on:
      agent-registry:
        condition: service_started
    volumes:
      - ./seed_system_agents.sh:/seed.sh:ro
      - ../platform/system-agents:/system-agents:ro
    entrypoint: ["bash", "/seed.sh", "/system-agents"]
    environment:
      - AGENT_REGISTRY_URL=http://agent-registry:8088
    networks:
      - agent-net

  # ------------------------------------
  # System Tools Seeder (one-shot)
  # ------------------------------------
  seed-system-tools:
    image: curlimages/curl:latest
    container_name: seed-system-tools
    depends_on:
      admin-api:
        condition: service_started
    volumes:
      - ./seed_system_tools.sh:/seed.sh:ro
      - ../platform/system-tools.yaml:/system-tools.yaml:ro
    environment:
      - ADMIN_API_URL=http://admin-api:8089
      - ADMIN_API_KEY=dev-admin-key
      - TOOLS_YAML=/system-tools.yaml
    entrypoint: ["sh", "/seed.sh"]
    networks:
      - agent-net

  # ------------------------------------
  # KG Service (port 8093)
  # ------------------------------------
  kg-service:
    build:
      context: ../../
      dockerfile: services/kg-service/Dockerfile
    container_name: kg-service
    ports:
      - "8093:8093"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # MCP Registry (port 8090) - MCP Client Hub
  # ------------------------------------
  mcp-registry:
    build:
      context: ../../
      dockerfile: services/mcp-registry/Dockerfile
    container_name: mcp-registry
    ports:
      - "8090:8090"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
      - PORT=8090
    depends_on:
      migrate:
        condition: service_completed_successfully
    networks:
      - agent-net

  # ------------------------------------
  # MCP Server (port 8091) - MCP Server Endpoint
  # ------------------------------------
  mcp-server:
    build:
      context: ../../
      dockerfile: services/mcp-server/Dockerfile
    container_name: mcp-server
    ports:
      - "8091:8091"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
      - SKILL_CATALOG_URL=http://skill-catalog:8087
      - SKILL_DISPATCHER_URL=http://skill-dispatcher:8085
      - PORT=8091
    depends_on:
      migrate:
        condition: service_completed_successfully
      skill-catalog:
        condition: service_started
      skill-dispatcher:
        condition: service_started
    networks:
      - agent-net

  # ------------------------------------
  # Admin API  (port 8089)
  # ------------------------------------
  admin-api:
    build:
      context: ../../
      dockerfile: services/admin-api/Dockerfile
    container_name: admin-api
    ports:
      - "8089:8089"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
      - ADMIN_API_KEY=dev-admin-key
      - TEMPORAL_HOSTPORT=temporal:7233
      - COOKBOOKS_DIR=/app/infra/platform/cookbooks
      - AGENT_REGISTRY_URL=http://agent-registry:8088
      - KG_SERVICE_URL=http://kg-service:8093
      - LITELLM_BASE_URL=http://litellm:4000/v1
      - LITELLM_CONFIG_PATH=/etc/litellm/config.yaml
    volumes:
      - ../../infra/platform/cookbooks:/app/infra/platform/cookbooks:rw
      - ./litellm_config:/etc/litellm:rw
    depends_on:
      migrate:
        condition: service_completed_successfully
      temporal:
        condition: service_started
      litellm:
        condition: service_started
    networks:
      - agent-net

  # ------------------------------------
  # Admin Console  (port 3001)
  # NOTE: Run on host with: cd apps/admin-console && npm run dev
  # ------------------------------------
  # admin-console:
  #   build:
  #     context: ../../
  #     dockerfile: apps/admin-console/Dockerfile
  #     args:
  #       NEXT_PUBLIC_ADMIN_API_URL: http://localhost:8089
  #   container_name: admin-console
  #   ports:
  #     - "3001:3001"
  #   environment:
  #     - NEXT_PUBLIC_ADMIN_API_URL=http://localhost:8089
  #   depends_on:
  #     - admin-api
  #   networks:
  #     - agent-net

  # ------------------------------------
  # Agent Studio  (port 3000)
  # NOTE: Run on host with: cd apps/agent-studio && npm run dev
  # ------------------------------------
  # agent-studio:
  #   build:
  #     context: ../../
  #     dockerfile: apps/agent-studio/Dockerfile
  #     args:
  #       # NEXT_PUBLIC_* vars are baked into the JS bundle at build time.
  #       # For local test: browser reaches backends via published ports on the host.
  #       NEXT_PUBLIC_API_GATEWAY_URL: http://localhost:8080
  #       NEXT_PUBLIC_TOOL_REGISTRY_URL: http://localhost:8086
  #       NEXT_PUBLIC_SKILL_CATALOG_URL: http://localhost:8087
  #       NEXT_PUBLIC_AGENT_REGISTRY_URL: http://localhost:8088
  #       NEXT_PUBLIC_KG_SERVICE_URL: http://localhost:8093
  #       NEXT_PUBLIC_TENANT_ID: default-tenant
  #   container_name: agent-studio
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - API_GATEWAY_URL=http://api-gateway:8080
  #     - KG_SERVICE_URL=http://kg-service:8093
  #   depends_on:
  #     - api-gateway
  #     - tool-registry
  #     - skill-catalog
  #     - agent-registry
  #     - kg-service
  #   networks:
  #     - agent-net

  # ------------------------------------
  # SRE Dashboard  (port 8501)
  # ------------------------------------
  dashboard:
    build:
      context: ../../
      dockerfile: services/dashboard/Dockerfile
    container_name: dashboard
    ports:
      - "8501:8501"
    environment:
      - API_GATEWAY_URL=http://api-gateway:8080
      - POSTGRES_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
    depends_on:
      - api-gateway
      - postgres
    networks:
      - agent-net

  # ------------------------------------
  # Workflow Service (port 8094) - Hybrid Workflow Registry & Dispatcher
  # ------------------------------------
  workflow-service:
    build:
      context: ../../
      dockerfile: services/workflow-service/Dockerfile
    container_name: workflow-service
    ports:
      - "8094:8094"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agentplatform?sslmode=disable
      - TEMPORAL_HOSTPORT=temporal:7233
      - PORT=8094
    depends_on:
      migrate:
        condition: service_completed_successfully
      temporal:
        condition: service_started
    networks:
      - agent-net

  # ------------------------------------
  # liteLLM Proxy (port 8000) - Unified LLM Interface
  # ------------------------------------
  litellm:
    image: ghcr.io/berriai/litellm:latest
    container_name: litellm
    ports:
      - "8000:4000"
    entrypoint: ["/bin/sh", "/litellm_start.sh"]
    environment:
      # API Keys
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      # Custom proxy support (for corporate gateway that may also use liteLLM)
      - CUSTOM_PROXY_URL=${CUSTOM_PROXY_URL:-}
      - CUSTOM_PROXY_KEY=${CUSTOM_PROXY_KEY:-}
      # Server settings
      - LITELLM_LOG=DEBUG
    volumes:
      - ./litellm_config:/etc/litellm:rw
      - ./litellm_start.sh:/litellm_start.sh:ro
    networks:
      - agent-net

networks:
  agent-net:
    driver: bridge

volumes:
  pg-data:
