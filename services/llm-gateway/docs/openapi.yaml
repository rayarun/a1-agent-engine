swagger: "2.0"
info:
  title: "LLM Gateway"
  version: "1.0.0"
host: "localhost:8083"
basePath: "/v1"
schemes:
  - "http"
paths:
  /chat/completions:
    post:
      summary: "Standardized Chat Completion"
      responses:
        200:
          description: "LLM response"
  /embeddings:
    post:
      summary: "Standardized Vector Embeddings"
      responses:
        200:
          description: "Embedding result"
