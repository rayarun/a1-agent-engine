swagger: "2.0"
info:
  title: "Sandbox Manager"
  version: "1.0.0"
host: "localhost:8082"
basePath: "/api/v1"
schemes:
  - "http"
paths:
  /execute:
    post:
      summary: "Execute code in an isolated container"
      responses:
        200:
          description: "Execution result"
